"""Main entry point for the CLI application."""

from app import CLI

def main():
    """Initialize and run the CLI application."""
    cli = CLI()
    cli.run()

if __name__ == "__main__":
    main() 